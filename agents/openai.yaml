interface:
  display_name: "Bisit"
  short_description: "Fix frontend design and accessibility issues"
  default_prompt: "Use $bisit on the selected text or file; if neither is supplied, audit and refine the current project's frontend while preserving content and behavior."

policy:
  allow_implicit_invocation: false
